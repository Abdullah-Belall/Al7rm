# دليل البدء السريع 🚀

## الخطوة 1: إعداد قاعدة البيانات

افتح PostgreSQL واعمل قاعدة بيانات جديدة:

```sql
CREATE DATABASE al7ram_support;
```

## الخطوة 2: إعداد Backend

```bash
# اذهب لمجلد Backend
cd backend

# ثبت الاعتماديات
npm install

# أنشئ ملف .env
```

انسخ هذا المحتوى في ملف `backend/.env`:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=al7ram_support

JWT_SECRET=my-secret-key-12345
JWT_EXPIRES_IN=24h

PORT=3001
NODE_ENV=development

FRONTEND_URL=http://localhost:3000
```

## الخطوة 3: إعداد Frontend

```bash
# اذهب لمجلد Frontend
cd frontend

# ثبت الاعتماديات
npm install
```

## الخطوة 4: تشغيل المشروع

افتح **Terminal 1** (للباك اند):

```bash
cd backend
npm run start:dev
```

افتح **Terminal 2** (للفرونت اند):

```bash
cd frontend
npm run dev
```

## الخطوة 5: التجربة

### 1. إنشاء حساب عميل:
- افتح المتصفح: `http://localhost:3000`
- اضغط "إنشاء حساب"
- أدخل:
  - البريد: `customer@test.com`
  - كلمة المرور: `123456`
- سيتم تسجيل الدخول تلقائياً

### 2. إنشاء حساب داعم:

**الطريقة 1: استخدام PowerShell (Windows)**
```powershell
cd scripts
.\create-supporter.ps1
```

**الطريقة 2: استخدام curl (Git Bash)**
```bash
curl -X POST http://localhost:3001/users/register-supporter -H "Content-Type: application/json" -d "{\"email\":\"supporter@test.com\",\"password\":\"123456\",\"name\":\"داعم تجريبي\",\"specialties\":[\"prayer\",\"guidance\"],\"maxConcurrentRequests\":5}"
```

**الطريقة 3: استخدام Postman/Thunder Client**

أو استخدم Postman/Thunder Client:
- URL: `POST http://localhost:3001/users/register-supporter`
- Body (JSON):
```json
{
  "email": "supporter@test.com",
  "password": "123456",
  "name": "داعم تجريبي",
  "specialties": ["prayer", "guidance"],
  "maxConcurrentRequests": 5
}
```

### 3. اختبار النظام:

**من حساب العميل:**
1. اضغط "طلب دعم جديد"
2. اختر فئة (مثلاً: "صلاة")
3. اضغط "إنشاء طلب"
4. انتظر حتى يتم تخصيص الطلب

**من حساب الداعم:**
1. افتح `http://localhost:3000/login` في نافذة خاصة (Incognito)
2. سجل دخول بالبريد: `supporter@test.com` وكلمة المرور: `123456`
3. اضغط "متاح" لتفعيل الحالة
4. ستظهر لك الطلبات المخصصة
5. اضغط "قبول" على الطلب
6. سيتم إنشاء مكالمة فيديو تلقائياً
7. اضغط "انضم للمكالمة"

**من حساب العميل:**
- ستظهر له زر "انضم للمكالمة" أيضاً
- اضغط عليه للانضمام

## ملاحظات مهمة ⚠️

1. **تأكد من تشغيل PostgreSQL** قبل تشغيل Backend
2. **افتح نافذتين منفصلتين** للمتصفح (واحدة للعميل، واحدة للداعم)
3. **للمكالمات الفيديوية**: المتصفح سيطلب إذن الكاميرا والميكروفون
4. **إذا لم تظهر الطلبات**: تأكد أن الداعم في حالة "متاح"

## استكشاف الأخطاء 🔧

### Backend لا يعمل:
- تأكد من تشغيل PostgreSQL
- تحقق من بيانات الاتصال في `.env`
- تأكد من تثبيت الاعتماديات: `npm install`

### Frontend لا يعمل:
- تأكد من تثبيت الاعتماديات: `npm install`
- تحقق من أن Backend يعمل على `http://localhost:3001`

### المكالمات لا تعمل:
- تأكد من منح المتصفح إذن الكاميرا والميكروفون
- جرب متصفح Chrome أو Edge (يدعمان WebRTC بشكل أفضل)

## أوامر مفيدة 💡

```bash
# تشغيل كل شيء معاً (من المجلد الرئيسي)
npm run dev

# إعادة تثبيت كل شيء
npm run install:all

# فحص حالة Backend
curl http://localhost:3001/health
```

