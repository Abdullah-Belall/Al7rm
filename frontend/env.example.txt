# API URL (Backend URL)
NEXT_PUBLIC_API_URL=http://localhost:3001

# WebSocket URL (should be same as API URL)
NEXT_PUBLIC_WS_URL=http://localhost:3001

# In production, use HTTPS:
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_WS_URL=https://api.yourdomain.com

# TURN Server Configuration (for WebRTC video calls across different networks)
# These are optional - the code uses public TURN servers by default
# For production, it's recommended to use your own TURN server
# Format: comma-separated list of TURN server URLs
# NEXT_PUBLIC_TURN_URLS=turn:your-turn-server.com:3478,turn:your-turn-server.com:3478?transport=tcp,turns:your-turn-server.com:5349
# NEXT_PUBLIC_TURN_USERNAME=your-turn-username
# NEXT_PUBLIC_TURN_CREDENTIAL=your-turn-credential

